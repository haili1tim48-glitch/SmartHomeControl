package com.example.smarthomecontrol;

import android.app.Activity;
import android.os.Bundle;

/**
 * Screen 2 – Expert Video playback (stub).
 * Receives the gesture label via Intent extra "EXTRA_GESTURE_LABEL".
 */
public class ExpertVideoActivity extends Activity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // Screen 2 implementation will go here
    }
}
